global:
  scrape_interval: 15s
  evaluation_interval: 15s
scrape_configs:
- job_name: blackbox_health_probes
  metrics_path: /probe
  params:
    module: [http_2xx]
  static_configs:
  - targets:
    - http://dev-rag-api:8000/health
    - http://dev-rag-api:8000/mcp/health
    - http://dev-webapp:80/
  relabel_configs:
  - source_labels: [__address__]
    target_label: __param_target
  - source_labels: [__param_target]
    target_label: instance
  - target_label: __address__
    replacement: dev-blackbox-exporter:9115