apiVersion: v1
kind: ConfigMap
metadata:
  name: rag-api-config
  labels:
    {{- include "rag-api.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
