apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-provider-web-ingress
  namespace: healthcare-ai
spec:
  podSelector:
    matchLabels:
      app: provider-web
  policyTypes:
  - Ingress
  ingress:
  - from:
    - namespaceSelector: {}
    ports:
    - protocol: TCP
      port: 80